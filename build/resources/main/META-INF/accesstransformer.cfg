# Expose the save storage access required to resolve the selected dimension's
# path. Forge already provides forgeGetWorldMap()/markWorldsDirty() for the
# runtime level map, so no additional members are opened.
public net.minecraft.server.MinecraftServer f_129744_ # storageSource

# Disposable battlefield levels must release save-side region handles without
# calling ServerLevel.close(), because close() force-saves every loaded chunk
# and can block the live server tick loop indefinitely.
public net.minecraft.server.level.ServerLevel f_143244_ # entityManager
public net.minecraft.world.level.entity.PersistentEntitySectionManager f_157493_ # permanentStorage

# Remove the vanilla/created border delegate when a disposable ServerLevel is
# detached, preventing one retained listener per round.
public net.minecraft.world.level.border.WorldBorder f_61905_ # listeners
public net.minecraft.world.level.border.BorderChangeListener$DelegateBorderChangeListener f_61864_ # worldBorder

# VVP/FCP 特化版（更改特化版）跨包直接调用 SBW FastThrowableProjectile 的这些
# protected 访问器（SmallCannonShellVehicleDamageHandler 等）。此前由
# vehicle_addition 的 accesstransformer 放宽；该 mod 移除后若不放宽，
# 服务器会在炮弹命中载具时抛 IllegalAccessError 并崩溃。SBW 方法名在
# srg/mojmap 下一致，无需映射转换。
public com.atsuishio.superbwarfare.entity.projectile.FastThrowableProjectile getDamageValue()F
public com.atsuishio.superbwarfare.entity.projectile.FastThrowableProjectile setDamageValue(F)V
public com.atsuishio.superbwarfare.entity.projectile.FastThrowableProjectile getExplosionDamageValue()F
public com.atsuishio.superbwarfare.entity.projectile.FastThrowableProjectile setExplosionDamageValue(F)V
